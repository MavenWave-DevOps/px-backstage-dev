
apiVersion: v1
kind: Service
metadata:
  name: backstage-service
  namespace: backstage
spec:
  type: ClusterIP
  ports:
    - port: 443
      targetPort: 7007
  selector:
    app.kubernetes.io/name: backstage-deployment

